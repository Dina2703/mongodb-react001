const express = require("express");
const router = express.Router();
const Note = require("../models/noteModel");

//configure a route to GET or SEND data from the database to the frontend.

module.exports = router;
